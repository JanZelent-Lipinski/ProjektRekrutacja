<Window x:Class="ProjektRekrutacja.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjektRekrutacja"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel Margin="10">

            <TextBox Width="300"
             Height="30"
             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />

            <DataGrid ItemsSource="{Binding Products}"
          AutoGenerateColumns="False"
          Margin="0,10,0,0"
          Height="250">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nazwa" Binding="{Binding Name}" />
                    <DataGridTextColumn Header="Cena" Binding="{Binding Price}" />
                    <DataGridTextColumn Header="Stan" Binding="{Binding Quantity}" />
                    <DataGridTextColumn Header="Jednostka" Binding="{Binding Unit}" />
                </DataGrid.Columns>

            </DataGrid>

            <TextBlock Margin="0,10,0,0"
               FontSize="16"
               Text="{Binding SearchText}" />

        </StackPanel>



    </Grid>
</Window>
